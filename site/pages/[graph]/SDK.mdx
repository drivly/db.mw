# SDK

You can import directly via a URL in the browser:

```javascript copy
import { db } from 'https://pkg.do/db.mw'

console.log(db.graph)
```

Or you can add from NPM:

```bash copy 
npm install db.mw
```

or 

```bash copy
yarn add db.mw
```
